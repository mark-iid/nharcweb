# build output
dist/
# dependencies
node_modules/
# generated types
.astro/
# environment
.env
.env.production
# macOS
.DS_Store
# logs
npm-debug.log*
# deploy secrets
deploy/*.key

# Claude Code local config (personal, not needed for build)
.claude/

# Source photos (originals) — only the optimized versions in public/ ship
pictures/
